# r05-b — judge's verdict

*Measured before NOTES.md was opened: private headless Chrome (CDP, own
harness), gzip serving on 8842, Fast 3G by protocol, 390 px by device metrics,
real key events for the keyboard loop. Compared side by side with the live
baseline at roleatlas.sennamind.com.*

| | score |
|---|---|
| Ease (25) | **22.5** |
| Curation-legibility (25) | **23.5** |
| Representation (20) | **17** |
| **Judge total (70)** | **63** |

## Measures, re-run

- **M1 — pass.** First card in DOM at **630 ms** median; the page's own stamp
  says 637–644. Claim of 625 honest. Incremental render lands the rest without
  blocking.
- **M2 — pass.** The fold carries the read-count headline, the five gates with
  counts, the funnel link and the roles-on-their-own-boards stamp. Both
  questions answerable — after more reading than the other two ask for.
- **M3 — pass.** No presets, so the honest count is **6 clicks** (two selects,
  one expand, three applies), ~4 s wall, three greenhouse tabs. Matches the
  claim exactly.
- **M4 — the judgement call holds.** I re-ran the audit at block granularity:
  the 11 residue hits are ordinary adjectives inside provenance-labelled
  description lines ("Uses AI models to design new medicines") or the sheet's
  own quoted refusals, each with a receipt in the surrounding block. The build
  auditing its own memos (`hype_audit()`, count printed on the sheet) is the
  right instinct and I'd keep it. Pass.
- **M5 — pass.** Null amounts render as nothing; visa silence prints as "that
  is silence, not a no" with the count.
- **M6 — pass on its own terms, with one real doctrine violation next door.**
  Keeps survive reload with their dates (fixing r04-b's chip miss); the ask is
  correctly day-gated (no same-day fire; fires once next day; dies on the
  answer). **But**: open the shortlist *link* in a clean browser and the page
  silently writes `kept_at: today` for every URL-carried company into the
  visitor's storage and shows "MY SHORTLIST … kept Aug 5, 2026". The visitor
  never kept anything. That is the page's hand rendered as the reader's verb —
  the exact move r04-a was charged for, one degree removed. a discloses the
  same hydration in a sentence; c refuses to hydrate at all. NOTES.md's "no
  localStorage needed — the #k= fragment carries them" made me check twice:
  storage *is* written. This cost representation points.

## The four signals

1. **WHAT / FOR WHOM / WHY THEM** — stacked at the masthead thesis's own
   setting, the largest and most readable description type of the round, with
   the varying provenance fact per card and the machine-authorship argued once
   in the lede — the closest reading of "say so once, well" among the three.
   The 418 absent get the best-informed absent state: the board's own unedited
   team words in the mono voice "this page reserves for things somebody else
   wrote," and "A BACKLOG IN SCRIPTS/DESCRIBE.PY — NOT A JUDGEMENT ABOUT THEM."
2. **Status** — one hairline frame, four possible words, no rule keyed off the
   value, and the lede argues it: "a company that already went public is a
   fact, not a warning." The YC caption's "the batch, the status and the 300
   people it lists are YC's numbers, not mine" is exactly right. Clean pass.
3. **The target** — 63. Even with round 4's winner; the inventions below are
   real but the fold and the link-keep take back what they earn.
4. **The baseline** — the tokens are all here (crop marks, micro-labels, one
   red argued and *corrected* — "at rest a count is ink" is a better red
   discipline than the baseline's own), and the right-rail OPEN ROLES stamp is
   the most register-like object on any card this round. But the cold fold
   spends ~40% of its height on a two-column memo, and the first card starts at
   **643 px and does not finish by 900**. Next to the live site the page reads
   as the *report about* the register rather than the register's next
   instrument. Authored, yes; essay-heavy, also yes.

## What this page invented that the others should take

- **Per-cut provenance recomputation:** "I have read the sites of 90 of these
  201. The other 111 say *not yet read*…" — the round's best
  curation-legibility device, recomputed at query time.
- **The RATE row:** "300 ÷ 98 = one opening per 3.1 people" — the only variant
  to surface hiring intensity per staff (round 4's gap #4), as a stated
  division of two sourced numbers, never a score.
- **The record sentence** is the fullest arithmetic of the three ("…you have
  opened nothing at the 61 companies here that CB Insights tracks").
- **Sticky controls** over a 732-card scroll — alone in the round.
- The keyboard sheet with the pre-boot key-queue note; kept dates on shortlist
  rows; the two-word giants switch ("HIDING THE GIANTS / SHOWING ALL 789").

## Where the points went

Ease −2.5: six clicks (no priced presets), the unfinished first card, and the
self-confessed 1,459 px phone header. Curation −1.5: the argument is complete
but front-loaded as prose; receipts sit mid-paragraph on the fold rather than
on the row. Representation −3: the silent link-keep inference (the round's one
doctrine violation), "companies I am applying to" putting a verb in the
reader's mouth, and the memo's showpiece card cut by its own fold.
